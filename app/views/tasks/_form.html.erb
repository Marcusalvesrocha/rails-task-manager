
<%= form_with model: task do |t|%>
  <% unless task.new_record? %>
    <%=t.check_box :completed %>
  <% end %>

  <%= t.label :title%>
  <%= t.text_field :title%>

  <%= t.label :details%>
  <%= t.text_field :details%>


  <%= t.submit %>
<% end %>
